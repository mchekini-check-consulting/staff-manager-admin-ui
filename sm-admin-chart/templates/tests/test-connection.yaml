apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
