version: '2'
services:
  staff-manager-api:
    container_name: staff-manager-admin-ui
    image: mchekini/staff-manager-admin-ui:1.0
    ports:
      - "81:80"

networks:
  default:
    external:
      name: staff-manager